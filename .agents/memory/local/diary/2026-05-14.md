# 2026-05-14

## install.bat + config wizard

- Поправил `install.bat`: `python -m pip` везде, проверки errorlevel, явный fallback на `pip install pyyaml` если `import yaml` не работает.
- Добавил в `config_wizard.py` шаг ввода `domain` (с пустым значением по умолчанию).
- Унифицировал порядок ключей в `config.example.yaml` и в выводе мастера: `domain → targets → credentials → concurrency → ports_file → exclusions`.

## Факты по проекту

- Мастер пишет `config.yaml` через `yaml.dump(..., sort_keys=False)` — порядок определяется порядком добавления ключей в dict в `create_config()`.
- `domain` используется в `main.py` для подкаталогов `output/<domain>` и `log/<domain>`; пустой допустим (fallback на `output/` и `log/`).

## SSH/inventory: правки enrichment

- Поправил `src/connectors/ssh.py`: валидация hostname (RFC) и os/distribution/kernel (single-line). Embedded-устройства с собственным CLI больше не пишутся как Linux, мусор в hostname отрезается.
- Поправил `src/enrichment.py`: SSH стал приоритетным протоколом для всех категорий хостов (был порядок `[winrm, psexec, ssh]` для windows). Группа `windows_ssh` теперь будет заполняться.
- Добавил soft-upgrade в `enrich_host`: если хост `completed` и не через SSH, но открыт 22 — пробуем SSH без `--force`. Это закрывает кейс «добавил ssh на windows-хост → хочу чтобы при следующем запуске он попал в windows_ssh».
- Переименовал группу `linux_servers_ssh` → `linux_ssh` в [src/reporting.py](../../../../src/reporting.py) и тестах.

## Web-эвристика: расширение по результатам ad-hoc разведки

- Прошёлся пробой по 22 хостам со статусом `scanned` + открытыми http/https. Нашёл повторяющиеся сигнатуры: `Virata-EmWeb`, `Allegro-Software-Rompager`, `RAID HTTPServer`, `WMI V*` (Mobotix), `gSOAP/*` (ONVIF), `MiniServ` (Webmin), TLS handshake failures на старых embedded, паттерн nginx+8080 (предположительно VoIP/PBX/NAS).
- Добавил `SERVER_HEADER_SIGNATURES` и `REALM_MODEL_RE` в [src/vendor_db.py](../../../../src/vendor_db.py); подключил в `classify_host` и `determine_vendor_model`. `www_authenticate` теперь попадает в `collect_host_text`.
- В [src/web_probe.py](../../../../src/web_probe.py): server=gSOAP всегда триггерит ONVIF SOAP probe, список ONVIF портов расширен (80/8899/5000/9090).
- 6 новых тестов в [tests/test_classification.py](../../../../tests/test_classification.py), 79/79 зелёные.
- Перенёс ad-hoc-скрипт в `tmp/probe_scanned.py` (по новому правилу про tmp/ в памяти).

## Storage переработка (на следующий день, 2026-05-15)

- Discovery больше не teardown'ит state. Новый метод `Storage.apply_discovery_snapshot(records, force)` с понятной семантикой: добавить/заменить/оставить как есть в зависимости от статуса узла и `--force`.
- Хосты, не найденные в этом прогоне discovery, остаются в state.
- `--force` теперь смысл: «делать полную замену discovery-узлов даже для уже завершённых». Из enrichment-этапов `force` убран (они и так обрабатывают только незавершённые).
- `scan_state.json` обзавёлся `meta`-блоком с `last_scan` и `version`. Backward-compat для плоского формата сохранена.
- При `Storage.__init__` автоматический бэкап в `output/<domain>/backups/`, ротация 30 файлов.
- Тесты: переписан `test_models_storage` под новую семантику (+ 3 новых кейса), `force=True` убран из `test_enrichment`. 82/82 зелёные.
