# Memory Index

Последняя оптимизация: 2026-05-14

## Проект

- net-conf-gen — генератор сетевых конфигов (Python)
- Точка входа: `main.py`
- Установка: `install.bat`
- Пример конфига: `config.example.yaml`

## Инфраструктура

- Платформа: Windows
- venv активируется в `install.bat`

## Ограничения

- Без README/CHANGELOG если не просили
- ⚠️ **Репозиторий публичный, и память (`.agents/memory/`) коммитится вместе с кодом** (НЕ в .gitignore). Любая запись в память уходит в публичный git. Запрещено писать: пароли, токены, ключи; реальные IP/хосты/FQDN/логины пользователя; имена доменов клиентов; имена внутренних серверов; пути к чувствительным файлам. Только обезличенные факты, рецепты и грабли. Перед каждой записью в diary/topics/MEMORY.md мысленно проверять: «это нормально опубликовать?». Если нет — не писать или обезличить.
- ⚠️ **Одноразовые/временные скрипты и файлы — только в `tmp/`** (в .gitignore). После использования удалять. Не оставлять разовые probe/dump/explore-скрипты в корне или в `src/`. Применять к любым ad-hoc разведкам, экспериментам, дампам данных.

## Факты

- При отсутствии конфига запускается интерактивный мастер настройки

## Грабли и уроки

## Ссылки

- [topics/config.md](topics/config.md) — порядок ключей конфига, грабли установки pyyaml
- [topics/enrichment-and-inventory.md](topics/enrichment-and-inventory.md) — порядок протоколов, soft-upgrade на SSH, группы инвентаря, валидация ответов SSH
- [feedback_style.md](feedback_style.md) — избегать англицизмов в ответах
